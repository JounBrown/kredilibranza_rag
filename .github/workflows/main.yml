name: Python CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del código
      - name: Checkout code
        uses: actions/checkout@v2

      # 2. Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'

      # 3. Instalar dependencias
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          cd Backend
          poetry install

      # 4. Cargar el archivo .env desde la ruta correcta
      - name: Set up environment variables
        run: |
          if [ -f "Backend/.env" ]; then cp Backend/.env .env; fi
          if [ -f "Backend/test/.env" ]; then cp Backend/test/.env .env; fi
          echo "PYTHONPATH=$(pwd)" >> $GITHUB_ENV
        working-directory: Backend

      # 5. Ejecutar pre-commit para análisis de código
      - name: Run pre-commit
        run: |
          cd Backend
          poetry run pre-commit run --all-files

      # 6. Ejecutar pruebas con pytest y generar reporte de cobertura
      - name: Run tests
        run: |
          cd Backend
          poetry run python -m dotenv run pytest --cov=app --cov-report=xml test/cases_test.py
